{
  "tasks": {
    "build": "deno task build:hasundue && sudo nixos-rebuild",
    "build:hasundue": "deno run -A users/hasundue/**/*.build.ts",
    "check": "deno task check:deno",
    "check:deno": "deno fmt --check && deno lint && deno check ./**/*.ts && deno test -A",
    "switch": "deno task build switch --flake .",
    "test": "deno task check && deno task build test --flake .",
    "update": "nix flake update ."
  },
  "lock": false
}
