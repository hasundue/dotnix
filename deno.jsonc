{
  "tasks": {
    "check": "deno check ./**/*.ts",
    "test": "deno task check && git add --all && nixos-rebuild test --flake .",
    "switch": "nixos-rebuild switch --use-previous-result --flake ."
  }
}
