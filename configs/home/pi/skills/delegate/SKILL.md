---
name: delegate
description: Agent tool prerequisite — read before delegating work to any subagent (Explore, Plan, general-purpose).
---

# Parameters

For `Explore`, override the defaults with:

```
model: "opencode-go/deepseek-v4-flash"
thinking: "off"
```

`general-purpose` and `Plan` inherit yours — omit `model`/`thinking`.
